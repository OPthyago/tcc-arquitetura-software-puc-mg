const prestadorPath = require('./prestador/prestadorPath');
const prestadorGetByIdPath = require('./prestador/prestadorGetByIdPath');
const associadoPath = require('./associado/associadoPath');
const associadoGetByIdPath = require('./associado/associadoGetByIdPath');

module.exports = {
	prestadorPath,
	prestadorGetByIdPath,
	associadoPath,
	associadoGetByIdPath
}
